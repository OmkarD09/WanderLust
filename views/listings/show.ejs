<% layout('/layouts/boilerplate') %>
<body>
    <div class="row mt-3">
        <div class="col-8 offset-2">
            <h3>&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                <%= listing.title %></h3>
        </div>
        <div class="col-6 offset-3 show-card">
            <img src="<%= listing.image.url %>" class="card-img-top show-img" alt="<%= listing.title %>" style="height: 25rem;">
            <div class="card-body">
              <p class="card-text">
                <%= listing.description %> <br>
                &#8377; <%= listing.price.toLocaleString("en-IN") %> <br>
                <%= listing.location %> <br>
                <%= listing.country %>
              </p>
              <br>
            </div>
        </div>
        <br>
        <div class="btns col-6 offset-3 mb-3">
            <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark edit-btn">Edit</a>
            &nbsp; &nbsp; &nbsp;
            
            <form style="display: inline;" action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
                <button class="btn btn-dark delete-btn">Delete</button>
            </form>
        </div>

        <hr>

        <div class="reviews col-6 offset-3 mb-3">
            <h4>Leave a Review</h4> 
            <form action="/listings/<%= listing._id %>" method="POST" novalidate class="needs-validation">
                <!-- <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" class="form-control" id="name" name="reviewer" required>
                </div> -->
                <div class="form-group">
                    <label for="rating">Rating</label>
                    <input type="range" min="1" max="5" class="form-control" id="rating" name="review[rating]" min="1" max="5" required> 
                    <div class="invalid-feedback">Please provide a rating between 1 and 5.</div>
                </div>
                <div class="form-group">
                    <label for="comment">Comments </label>
                    <textarea class="form-control" id="comment" name="review[comment]" rows="3" required></textarea>
                    <div class="invalid-feedback">Please add some comments for your review.</div>
                </div>
                <br>
                <button type="submit" class="btn btn-dark">Submit Review</button>
            </form>
        </div>

        <hr>
        <div class="reviews col-6 offset-3 mb-3">
            <h4>All Reviews</h4>
            <div class="row">
            <% if(listing.reviews.length === 0) { %>
                <p>No reviews yet. Be the first to review!</p> 
            <% } else { %>
                <% for(let review of listing.reviews) { %>
                    <div class="card-review mb-3 ms-3 col-5">
                        <div class="card-body">
                            <h5 class="card-title mb-1">Rating: <%= review.rating %> / 5</h5>
                            <p class="card-text mb-1" ><%= review.comment %></p>
                            <p class="card-text"><small class="text-muted">Reviewed on <%= review.createdAt.toDateString() %></small></p>

                    <form action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="POST">
                        <button class="btn btn-sm btn-dark">Delete</button>
                    </form>  
                     </div>
                    </div>
                <% } %>
            <% } %>
            </div>
        </div>
        <br>
        <br>
    </div>
    <%- include('../includes/footer.ejs') %>
        <script>
        // Example starter JavaScript for disabling form submissions if there are invalid fields
        (() => {
            'use strict'

            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            const forms = document.querySelectorAll('.needs-validation')

            // Loop over them and prevent submission
            Array.from(forms).forEach(form => {
                form.addEventListener('submit', event => {
                    if (!form.checkValidity()) {
                        event.preventDefault()
                        event.stopPropagation()
                    }

                    form.classList.add('was-validated')
                }, false)
            })
        })()
    </script>

</body>
