<% layout('/layouts/boilerplate') %>
<body>
    
    <ul>
        <li><h1><%= listing.title %></h1></li>
        <li><p>Description: <%= listing.description %></p></li>
        <li><p>Image: <img src="<%= listing.image.url %>" alt="<%= listing.title %>" /></p></li>
        <li><p>Price: $<%= listing.price %></p></li>
        <li><p>Location: <%= listing.location %></p></li>
        <li><p>Country: <%= listing.country %></p></li>
    </ul>

    <form action="/listings/<%= listing._id %>/edit" method="GET"">
        <button type="submit">Edit Listing</button>
    </form>

    <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete Listing</button>
    </form>

    <%- include('../includes/footer.ejs') %>

</body>

